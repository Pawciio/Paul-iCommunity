import styled from "styled-components";

const H1 = styled.h1`
  margin: 10px;
  font-size: 16rem;
  text-shadow: 0px 8px 4px rgba(0, 0, 0, 0.4);
  font-weight: bolder;
  color: white;
  text-transform: uppercase;
`;

export default H1;
